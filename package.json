{
  "name": "brandvm",
  "version": "1.1.1",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.25.0",
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "dev": "node build.mjs --dev",
    "build": "node build.mjs",
    "check": "tsc --noEmit",
    "test": "node --test tests/*.test.cjs",
    "test:browser": "playwright test",
    "test:webflow": "pnpm build && node scripts/check-webflow.mjs",
    "validate": "pnpm check && pnpm build && pnpm test && pnpm test:browser"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "@types/jquery": "^3.5.32",
    "esbuild": "0.28.2",
    "swiper": "^11.2.10",
    "typescript": "7.0.2"
  }
}
