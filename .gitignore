node_modules/
.DS_Store

# These repos are public — never let a key reach a commit.
.env
.env.*

# Production JS, CSS and version.json stay tracked so every release tag works.
dist/*.map

# Local browser test reports; never part of the site assets.
test-results/
playwright-report/
